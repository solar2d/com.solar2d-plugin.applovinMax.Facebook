Sources for the plugin `plugin.applovinMax.Facebook`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Facebook" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
